!
! VTP configured with MD5-authenticated password.
! `vtp password X` causes the parser to set vtp.password_set=true.
!
hostname rtr-l2-compliant
!
vtp mode transparent
vtp password Vtp$ecret2026!
!
spanning-tree mode rapid-pvst
!
end
